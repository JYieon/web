<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <table border="1">
        <tr>
            <th>아이디</th><th>이름</th><th>주소</th>
        </tr>
        <% for(let i = 0; i<list.length; i++) { %>
            <tr>
                <td><%= mem[i]["id"] %></td>
                <td><a href="/member/info?id=<%= list[i]['id'] %>"><%= mem[i].name %></a></td>
                <td><%= mem[i]["addr"] %></td>
            </tr>
        <% } %>
        
    </table>
</body>
</html>